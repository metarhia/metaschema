Dictionary({
  Name: { domain: 'Nomen', required: true },
  Act: Action(
    (session, { Id }, callback) => callback(null, `Resource${Id}`)
    // `Form` will be resolved as 'SchemaWithActions.Act'
  ),
  Check: Validate(({ Id }) => true),
})
