({
  SimpleField: { domain: 'SimpleDomain' },
  Date: {
    type: 'object',
    class: 'Date',
    parse: () => null,
    check: x => !Number.isNaN(+x),
  },
  Validate: Validate(() => null),
  NativeFunc: Date.parse,
});
