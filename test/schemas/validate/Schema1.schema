{
    Name: { domain: 'Nomen', required: false },
    Ratio: { domain: 'Ratio', required: false },
    DoSome: Action({
      Execute: async (session, ctx, args) => args.Name,
      Args: {
        Name: { domain: 'Nomen' },
      },
      Returns: {
        Name: { domain: 'Nomen' },
      },
    }),
}
